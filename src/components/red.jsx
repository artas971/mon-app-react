import React from "react";
import styled from 'styled-compenents'

const Red =() =>{
    <React.Fragment>
    return <mainContainer>Hello world again !</mainContainer>;
    <Red />
    </React.Fragment>
};
const mainContainer = styled.h2 `
`
export default red ;